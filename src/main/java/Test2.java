public class Test2 {
    //Emp -> {id, name, age,dep} -> list<EMp> , Map<Dep ,ListEMp>
    // List<Emlployee> empList  -> age,
    // emplList.stream().sort(Employee::getAge).collect(Collectors.toList());
    // Map<String, List<Employee>> depEmp=emplist.stream().collect(groupBy(Employee::getDep));


    //Get ---> end
    // Create --> post
    // put ->update

    // @PostMapping(/create/user)
    //public ResponseEnity<Object> createUser(@RequestBody @Valid UserRequestDto){

    // Service layer call
    // Parsing Layer call
    // return response

    // }


    public static void main(String[] args) {

    }
}
